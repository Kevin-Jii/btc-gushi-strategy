import { Activity, Bot, Database, ReceiptText } from "lucide-react";
import type { ReactElement } from "react";
import type { DashboardState } from "../../../src/dashboard/dashboard-types";
export function ActivityTimeline({ state }: { state: DashboardState }): ReactElement { return <section className="terminal-panel p-4"><div className="flex items-center gap-2"><Activity size={16} className="text-cyan" /><h2 className="terminal-title">运行日志</h2><span className="ml-auto text-[10px] text-muted">最近 {Math.min(state.activity.length, 6)} 条</span></div><div className="mt-3 grid max-h-43 gap-1 overflow-auto">{state.activity.slice(0, 6).map((item) => <div key={item.id} className="grid grid-cols-[24px_minmax(0,1fr)_auto] items-center gap-2 border-b border-line py-2"><span className="text-muted">{item.type === "ai" ? <Bot size={13} /> : item.type === "order" ? <ReceiptText size={13} /> : <Database size={13} />}</span><div className="min-w-0"><strong className="block truncate text-[11px]">{item.title}</strong><span className="block truncate text-[9px] text-muted">{item.detail}</span></div><time className="font-mono text-[9px] text-muted">{new Date(item.at).toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit" })}</time></div>)}{state.activity.length === 0 && <div className="py-8 text-center text-xs text-muted">暂无运行日志</div>}</div></section>; }
